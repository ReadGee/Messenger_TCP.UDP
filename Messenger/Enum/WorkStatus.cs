namespace Messenger.Enum
{
    public enum WorkStatus
    {
        NONE,
        ID,
        PASS,
        PASSHESH,
        END,
        BEGIN,
        ENDSERVER,
        PING,
        OK,
        NOT,
    }
}
